<?xml version="1.0" encoding="UTF-8"?>
<metadata>
  <plugins>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>github-release</prefix>
      <artifactId>github-release-maven-plugin</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>sql-execute</prefix>
      <artifactId>sql-execute-maven-plugin</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>twitch-release</prefix>
      <artifactId>twitch-release-maven-plugin</artifactId>
    </plugin>
  </plugins>
</metadata>
