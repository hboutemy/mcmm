<?xml version="1.0" encoding="UTF-8"?>
<metadata>
  <plugins>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>graalhelper</prefix>
      <artifactId>graalhelper</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>htmlcompiler</prefix>
      <artifactId>htmlcompiler</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>jooq-preprocessor</prefix>
      <artifactId>jooq-preprocessor</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>slate4j</prefix>
      <artifactId>slate4j</artifactId>
    </plugin>
  </plugins>
</metadata>
