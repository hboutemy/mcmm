<?xml version="1.0" encoding="UTF-8"?>
<metadata>
  <plugins>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>jenkins-dependency</prefix>
      <artifactId>jenkins-dependency-maven-plugin</artifactId>
    </plugin>
    <plugin>
      <name>${project.groupId}:${project.artifactId}</name>
      <prefix>windows-symbol-store</prefix>
      <artifactId>windows-symbol-store-maven-plugin</artifactId>
    </plugin>
  </plugins>
</metadata>
